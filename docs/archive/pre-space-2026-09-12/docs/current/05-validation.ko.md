# 5. 테스트·출시 체크리스트

[English](05-validation.en.md)

> **과거 기록 · 2026-09-12 폐기/대체.** 현재 게임은 [PSX 우주 배송 기획](../../../../current/01-overview.ko.md)을 따른다. 아래 구현·실행·아트·수치는 과거 자료이며 현재 상태가 아니다. 삭제된 파일은 경로 기록으로만 남긴다.


현재 메인 개발은 NO RETURNS Unity다. 아래 Godot 명세·검증은 보존된 기존 게임의 기준이며 Unity 구현 완료를 뜻하지 않는다. 현재 기준 / Current baseline (`18-unity-mainline.ko.md`; retired file).

검토일: 2026-09-09 · 게임 0.9.4 · 코드 기준: 이번 반응 장치 수정본.


체크 표시는 아래에 명시한 버전별 증거 범위만 의미한다. 0.9.3은 과거 기록이며, 이번 0.9.4의 새 검사 결과는 하단에 구분한다.

## 확인된 개발 기준점 — 0.9.3

- [x] 행동 검사 39개, 2인 온라인 시나리오 10개, 4인 시나리오 1개 통과 기록. `artifacts/animation-polish-suite.log`.
- [x] Windows 패키지 내보내기·실행 확인 기록. `artifacts/packed-smoke-output.log`, `artifacts/polish-packed-render.log`.
- [x] 16초·60fps 동작 녹화와 표본 검토. `artifacts/motion-polish.mp4`.
- [x] 물리 실험실 기존 운반 복원 검사. `artifacts/lab-revert-test.log`.

기록은 로컬 `artifacts/`에 있으며 Git에는 포함하지 않는다. 파일이 없거나 다른 버전으로 덮였으면 해당 증거를 재확인한다. 자동 검사와 표본 검토는 장시간 사람 플레이를 대체하지 않는다. 시스템 인증서 읽기 경고가 있었으므로 모든 로그가 무경고였다고 주장하지 않는다.

## 변경별 검사

| 변경 | 필요한 검사 |
|---|---|
| 문서 | 한영 대응·상호 링크·경로·버전·숫자·상태 일치 |
| 규칙·수치 | 관련 행동 검사, 기존 규칙과 변경 의도 비교 |
| 물리·운반·애니메이션 | 재현 실패→수정 통과, 실제 연속 동작과 접촉 검토 |
| 맵 | 시작점·경계·충돌·카메라·경로·배송·쥐 및 실제 협동 동선 |
| 통신 | 관련 2인/4인 시나리오, 버전·맵 불일치, 중복·지연·이탈 |
| 배포 | 내보내기·포함 파일·해시·패키지 실행, 변경된 사용자 문서 포함 |

문서 검사: `python tools/check_docs.py`. 전체 자동 검사: `python tools/run_tests.py`. 배포: `python tools/build_windows.py` 또는 `BUILD.cmd`. 테스트 실패를 수치 완화로 숨기지 않는다. 검증 결과에 명령·빌드·날짜·성공/실패·한계를 남긴다.

## 출시 전 미완료

ECO-01의 현재 산술은 4인 첫 성공 120 ≥ 전체 최대 강화비 110이다. 이는 정적 계산 확인이며 이번 게임 실행 결과가 아니다. 경제 조정 시 인원별 수입·구매 경로·두 구매 시점의 선택과 솔로 진행을 확인한다. 작성본과 분석 (`07-mda.ko.md`; retired file)의 Word 내용·구조는 검사했지만 페이지 렌더러 부재로 시각 검수는 미완료다.

사람 플레이의 재미 검수에서는 MDA 분석의 관찰 질문 (`07-mda.ko.md`; retired file)을 참고해 자발적 협동·운반 숙련·경로 발견·사고 수습·재시도 사례를 기록한다. 현재는 분석 가설이며 새 플레이 결과나 완료 근거가 아니다.

- [ ] 별도 Windows 기기 및 다른 네트워크에서 2/3/4인 플레이.
- [ ] 신규 4인 그룹의 온보딩·완주·재플레이 검수.
- [ ] 최신 빌드 성능 측정과 최소/권장 사양 확정.
- [ ] 최종 아트 권리·생성 콘텐츠 고지·라이선스 확인.
- [ ] Steam 계정·앱·초대·설치·상점·빌드 심사의 실제 상태 확인.
- [ ] 실제 기능과 일치하는 소개·스크린샷·영상·가격·지원 연락처.
- [ ] 사용자 출시 승인과 실제 공개/설치 확인.

Steam 세부 절차는 출시 작업 목록 (`../steam/05-release-checklist.ko.md`; retired file), 로드맵 (`../steam/01-release-roadmap.ko.md`; retired file)을 참조한다. 플랫폼의 비용·대기 기간·심사 요구사항은 실행 시 공식 Steamworks 문서에서 다시 확인한다. 이 문서는 현재 정책의 재검증이나 승인 증빙이 아니다.

## 0.9.4 점프대·충격 반응 검증 — 완료 결과와 한계

- [x] 실제 GLB의 스프링/안내봉 결합 메시를 확인하고 발판 두께·양 끝 접촉을 연속 구간 검사.
- [x] 문서 속도의 강도 차이, 회전된 부모에서도 충격의 월드 방향 유지, 상자 강체 이동과 초기화 검사.
- [x] 실제 렌더러로 60fps·241프레임을 녹화하고 압축·튀어 오름·흩어짐 표본을 검토. `artifacts/reactive-fix.mp4`.
- [x] 행동 41개·2인 10개·4인 1개 통과 결과 확인. 2인 검사 1개는 시간 초과 후 재검사 통과.
- [x] Windows 패키지 재생성, 기본 실행 검사 및 실제 그래픽 렌더러로 180프레임 실행 확인. `artifacts/reactive-build.log`, `artifacts/reactive-packed-render.log`.

첫 전체 검사에서는 충격 정보 추가로 기존 2,048바이트 캠페인 메타데이터 검사에 실패했다. 상태를 압축 전송하도록 수정하고 압축 해제 크기를 제한한 뒤 해당 검사를 통과했다. 전체 최종 로그는 `artifacts/reactive-fix-suite-final.log`이다. 장식 상자는 맵/장식 잔해와 충돌하며 직원/배송 화물을 직접 밀지 않는다. 잔해의 최종 위치 일치, 외부 네트워크와 장시간 실제 협동 품질은 미검증이다.

행동 검사 41개, 2인 시나리오 10개, 4인 시나리오 1개의 통과 결과를 확보했다. 전체 실행의 마지막 던지기 애니메이션 통신 검사에서 1회 시간 초과가 발생했고 단독 재검사에서 통과했다. 반복 안정성은 추가 관찰이 필요하다. Windows 빌드와 기본 실행 검사, 실제 렌더러의 180프레임 패키지 실행도 통과했다. 근거: `artifacts/reactive-fix-suite-final.log`, `artifacts/reactive-animation-network-retest.log`, `artifacts/reactive-four-final.log`, `artifacts/reactive-build.log`, `artifacts/reactive-packed-render.log`.


D안 추가 검증: 기존 41개 행동·2인 10개·4인 통신 검사 및 별도 D 아트 검사 통과. 실제 렌더 표본과 완료된 Windows 배포 검사는 D안 검증 기록 (`../art/10-approved-d.ko.md`; retired file)을 참조한다.

## 원정 기획 개정 2 — 문서 검증과 향후 검사 · 2026-09-10

이번 작업은 확장 기획 (`08-expansion-directions.ko.md`; retired file)의 한영 대응·경로·현재 코드와의 경계·시간 및 경제 산술을 검토하는 문서 작업이다. 게임 테스트·렌더·빌드·실제 플레이를 새로 실행하지 않는다. 문서 검사 통과를 아래 기능 검증으로 대체하지 않는다.

- [ ] 320개 기본 구성과 100개 시드에서 운반·수령·회수 및 특수 화물 없는 대체 경로 검사.
- [ ] 원정 지급·가격·4칸·저장·중복 사건·재접속·방장 이탈 검사.
- [ ] 솔로/2인/3인 각 1회, 신규 4인 그룹 2팀 각 2근무의 실제 플레이와 관찰 기록.
- [ ] 운반 중 의미 없는 이동/대기 20초 초과 구간, 기본 25~30분·추가 포함 30~40분 목표와 실제 시간 비교.

새로운 재미·성능·외부 네트워크·출시 준비 완료 증거는 아직 없다.

## EXP-01 실제 구현 · 2026-09-10

상태·저장 31개, 맵 API·1.8m 운반 여유 격자 연결·직선 시야 차단, 물리 수령·오배송·중도 귀환·다음 근무 검사 통과. 실제 별도 프로세스 로컬 ENet 2인·4인 배송/보상/귀환 일치 검사 통과. 근거: python tools/run_expedition_tests.py, artifacts/expedition*-output.log. 메뉴·게시판·플레이·맵의 GPU 표본은 artifacts/expedition-render.log와 expedition-*.png. 조명·시작 시야를 조정 후 재촬영. 실제 0.4초 규칙을 검사하며 테스트용 축약 없음. 최초 네트워크 시간 초과 후 출력 수집을 파일로 바꿔 재실행 통과. 환경 인증서/셰이더 캐시 경고는 남음. 사람 재미·외부 네트워크·장시간·저장 중 강제 종료는 미검증.

플레이 안내와 한계 (`../prototype/10-expedition.ko.md`; retired file).

최종 배포 검증: Windows 내보내기·기본 실행 검사 통과, --expedition으로 실제 GPU 배포본 120프레임 실행 종료 0(artifacts/expedition-packed-render.log). 원정 가이드 최신본 포함·SHA256·ZIP 무결성 확인. 문서 30개 검사 통과. 바로 실행 후 메인 메뉴 복귀가 반복 자동 진입하지 않는 test_expedition_boot.gd도 통과. 기존 인증서 저장소 경고 외 스크립트/장면 오류 없음.


## Unity 개발 환경 · 2026-09-11

Editor 6000.6.0f1, CLI 1.0.0-beta.9, URP 17.6.0, Pipeline 0.6.0-exp.1. 분리 프로젝트 생성·Hub 등록·C# 컴파일·개발 장면 생성·환경 검사·Windows 개발 빌드 성공(종료 0). CLI에서 열린 Editor의 ready 상태와 Development 장면의 오브젝트를 재조회했다. 배포본 30초 실행에서 D3D11·PhysX 초기화와 프로세스 생존을 확인한 후 검사 프로세스를 종료했다. 화면 품질·사용자 조작·게임 이식·온라인·출시 검증은 하지 않았다.

근거: `artifacts/unity/setup.log`, `build.log`, `player.log`, `hierarchy.json`. 초기 샌드박스 라이선스 연결 거부는 일반 사용자 실행으로 해결했다. URP 템플릿은 품질별 렌더러만 설정되어 기본 렌더러 검사가 실패했으며 PC 기본 렌더러를 명시한 뒤 재검사에 통과했다. Unity 종료 중 Mono 스레드/디버거 경고는 남지만 최종 초기화와 빌드는 종료 0이다. 문서 링크·언어 대응·새 환경 문서 수치를 확인했다.

환경 안내 (`14-unity-environment.ko.md`; retired file).


## UNITY-02 · 이동·운반과 공식 MCP · 2026-09-11

공식 Unity 스킬 13개를 설치하고 Codex Unity MCP 등록·경로 재검증·149개 도구 조회를 완료했다. 이 대화에서는 stdio MCP 실행 도구로 실제 tools/call 요청을 사용했다. MCP에서 CarryLab 생성·저장·재조회·플레이·HUD 화면 캡처를 수행했다. 기존 Development와 Godot 게임을 보존했다.

Unity 0.2.0에 이동·3인칭 카메라·점프·물리 들기·놓기·충전 던지기·낙하/수동 복구·HUD와 별도 실행 버튼을 추가했다. 물리 검사 24개 통과. 운반 이동 저하를 재현하고 손 속도 보정으로 해결했다. 기본 TMP 가져오기 시간 초과는 Editor 종료 후 설치 패키지 원본 37개를 GUID 보존으로 복원하여 해결했다. 빈 장면에서 HUD 메뉴를 실행한 오류는 CarryLab을 명시적으로 연 뒤 재검증했다.

최종 Windows 빌드 종료 0, 배포본 25초 실행에서 PhysX 초기화·프로세스 유지와 예외 없음 확인. 강제 종료한 검사 프로세스이므로 정상 종료 검증으로 기록하지 않는다. 한영 숫자 대응과 문서 링크 검사 통과. 근거: `artifacts/unity/test.log`, `build.log`, `carry-player.log`, `carry-hierarchy.json`, `carry-lab.png`. 사람의 전체 조작감·정식 애니메이션·온라인·출시 준비는 미검증이다. 현재 명세 (`15-unity-controls.ko.md`; retired file).


2026-09-12 · 새 Unity 배송 실험: NR-LOOP-01 실행·검증 (`22-slapstick-loop.ko.md`; retired file) — 구현·자동 검사·사람 검증의 구분은 해당 문서를 따른다.
